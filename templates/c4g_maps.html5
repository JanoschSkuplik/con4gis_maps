<?php if (!$this->mapData['mapDiv']): ?>
<div id="<?php echo 'c4gMap_' . $this->mapData['id'] ?>"></div>
<?php  endif; ?>

<?php // echo json_encode($this->mapData) ?>

<script>
//<![CDATA[

// C4GMaps(<?php echo json_encode($this->mapData) ?>);

	// var map_<?php echo $this->mapData['id'] ?> = new c4g.Map({
	// 	target: "<?php echo $this->mapData['mapDiv'] ?>",
	// 	baselayers: [
	// 					<?php #pass ?>
	// 				]
	// });

jQuery(document).ready(function() 
{
	// no use of "var", so it will become global
	c4gMap_<?php echo $this->mapData['id'] ?> = new c4g.Map(
			 <?php echo json_encode($this->mapData) ?>
		);
	// c4gMap_<?php echo $this->mapData['id'] ?>.run();
});
//]]>
</script>